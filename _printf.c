#include "holberton.h"
/**
 * _printf - Entry point
 * @format: the format
 */
void _printf(const format, ...)
{
	va_list data;
	unsigned int c1 = 0, c2 = 0;

	cases porcentage[] = {
		{"c", p_char},
		{"s", p_int},
		{"d", p_float},
		{"i", p_str},
		{NULL, NULL}
	};
	va_start(data, format);

}
